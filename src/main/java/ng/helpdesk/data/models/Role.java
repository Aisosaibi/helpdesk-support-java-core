package ng.helpdesk.data.models;

// These are the three supported user roles in the helpdesk application.
// The front end uses them to decide which tabs and actions are visible.
public enum Role {
    CUSTOMER,
    AGENT,
    ADMIN
}
